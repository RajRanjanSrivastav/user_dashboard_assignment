# user_dashboard_assignment
Angular standalone user dashboard with RxJS state management, Chart.js pie chart, lazy-loaded user form, and real-time updates. Includes search, pagination, and session storage persistence. Built with Tailwind CSS and optimized using deferred loading for better performance and scalability.

# Angular User Dashboard (Standalone + RxJS + Chart.js)

A modern Angular 14+ dashboard built with standalone components, RxJS state management, and Chart.js. It allows dynamic user management with real-time updates, lazy-loaded forms, and optimized rendering.

---

## 🚀 Features

- Add users via lazy-loaded modal form
- Dynamic user table with search & pagination
- Role distribution pie chart using Chart.js
- Real-time UI updates using RxJS BehaviorSubject
- Standalone Angular architecture (no modules)
- Deferred loading for performance optimization
- Session storage persistence (refresh-safe)
- Clean Material-inspired UI with Tailwind CSS

---

## 🛠 Tech Stack

- Angular 14+
- RxJS
- Chart.js
- Tailwind CSS
- TypeScript

---

## ⚙️ Setup

```bash
npm install
ng serve
